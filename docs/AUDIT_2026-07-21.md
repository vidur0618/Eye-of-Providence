# Dashboard data, policy, and design audit — 2026-07-21

## Outcome

The release passes its deterministic integrity checks, but the live research run is **partial**. The product now displays that state directly instead of implying exhaustive coverage.

## Resource-data run

| Category | Result | Coverage and boundary |
| --- | --- | --- |
| Facility registry and announcements | Update available | The frozen Epoch package is internally valid (74 total records, 65 U.S. records, 18 curated in the dashboard), but the remote ZIP changed after the cutoff. The last valid immutable snapshot was preserved for review. |
| State electricity | Blocked live refresh | The release retains 50 EIA 2024 state records. The local demo-key request returned HTTP 429; no partial replacement was published. State totals are context, not grid headroom or observed data-center load. |
| State water | Stale by design | The release retains the latest nationally consistent USGS state compilation used by the project: 2015, all 50 states. It is not facility water-source or entitlement evidence. |
| Drought | Passed | The U.S. Drought Monitor service returned 50 state responses for the release-valid weekly date. |
| Geography and reconciliation | Passed | Exact 50-state joins, Alaska/Hawaii geometry, release hashes, crosswalk copies, facility-to-state reconciliation, and policy approval gates passed. |

## Policy and legal run

- The public [NCSL 50-state data-center incentive index](https://www.ncsl.org/fiscal/subsidizing-servers-how-states-are-competing-to-attract-data-centers), updated April 1, 2026, yielded all 50 state rows and 38 dedicated-incentive candidates.
- All 38 records are labelled `candidate`, use legal status `unknown`, and are model-inactive. Thirty-two have an official citation candidate supplied by the index; six still require a primary link.
- The keyed [OpenStates v3](https://docs.openstates.org/api-v3/) run could not execute because `OPENSTATES_API_KEY` was absent. Therefore, this run does **not** claim current enumeration of every bill or draft.
- Zero state instruments have completed primary-source legal review. Zero state policy effects are approved.
- The source registry now includes EIA state electricity, USGS 2015 water use, U.S. Drought Monitor, OpenStates, NCSL, SEC EDGAR, Federal Register, Congress.gov, Regulations.gov, and the existing FERC/NERC/facility sources with purpose and limitation fields.

## Design and functionality audit

- Removed the duplicate eight-card capacity/resource ledger, second forecast chart, hardware block, and provenance wall from the dashboard.
- Forecast controls render only on the dashboard. Policy, sources, facilities, scenarios, and methods no longer carry irrelevant forecast controls above their content.
- Added a compact policy workbench with a functional state selector, 50-state candidate counts, selected-state evidence, official citation candidates, and secondary discovery links.
- Added a machine-readable data-run panel with explicit passed, stale, blocked, gap, credential-required, partial, and update-available states.
- Kept the synchronized map, three forecast paths, state evidence, source-lineage drawer, current-view CSV, citation copy, sortable 50-state table, URL state, comparison modes, playback, and reduced-motion behavior.
- Browser checks passed at 1440×900, 390 px, and 320 px without page-level horizontal overflow. The 50-state table remains intentionally contained in its own horizontal scroller on narrow screens.
- Policy-state selection, navigation, map-layer selection, period advance, URL synchronization, and citation copy were exercised in the live app. No browser console errors or warnings were recorded.

## Team-execution note

Three requested audit roles were launched for resource data, state policy/legal coverage, and design/functionality. The agent service rejected each launch because its refresh token had been revoked, so no sub-agent findings are represented as completed work. The same three audit scopes were completed serially in this task, and the infrastructure failure remains a disclosed limitation.

## Required next actions for an exhaustive claim

1. Configure `EIA_API_KEY` and `OPENSTATES_API_KEY` as server-side GitHub Actions secrets and run both review-only jobs.
2. Review the changed Epoch package as a new immutable snapshot; do not overwrite the current cutoff.
3. Link controlling primary text for every policy candidate, confirm current status and dates, and deduplicate mechanisms.
4. Add commission dockets, utility tariffs, agency decisions, local actions, and project announcements to the review inbox. A tax-incentive index alone is not comprehensive policy coverage.
5. Publish only after CI and a human reviewer approve hashes, all-state joins, provenance, legal links, and any proposed quantitative effect.
